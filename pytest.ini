[pytest]
asyncio_mode = auto
# Explicit "function" (not the session scope that caused a real bug
# earlier - see NullPool comment in app/core/database.py). Stated
# explicitly here to silence pytest-asyncio's deprecation warning about
# this being unset, not to change behavior - function was already the
# implicit default.
asyncio_default_fixture_loop_scope = function
